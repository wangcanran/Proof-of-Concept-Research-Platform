# 复制为 backend/.env 并填写（勿将 .env 提交到 Git）
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=research_system
PORT=3002

# 设为 false 时新用户为「未激活」，需管理员审核后才能登录；默认（不设置）为注册即激活
# REGISTER_DEFAULT_ACTIVE=false

# 前后端不同源时填写（逗号分隔），同域 Nginx 反代可省略
# CORS_ORIGINS=https://你的前端域名
